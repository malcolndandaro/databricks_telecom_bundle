resources:
  pipelines:
    pipeline_fact_subscription:
      name: fact_subscription
      configuration:
        confs.p_catalog: ${var.catalog_name}
      libraries:
        - notebook:
            path: ../facts/fact_subscription.sql
      target: billing_gold
      development: true
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true