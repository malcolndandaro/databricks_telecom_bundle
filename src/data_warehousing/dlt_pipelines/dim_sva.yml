resources:
  pipelines:
    pipeline_dim_sva:
      name: dim_sva
      configuration:
        confs.p_catalog: ${var.catalog_name}
      libraries:
        - notebook:
            path: ../dimensions/dim_sva.py
      target: customer_gold
      development: true
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true
