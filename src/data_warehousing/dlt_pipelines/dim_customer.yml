resources:
  pipelines:
    pipeline_dim_customer:
      name: dim_customer
      configuration:
        confs.p_catalog: ${var.catalog_name}
        confs.p_schema_customer_gold: ${resources.schemas.customer_gold.name}
        confs.p_schema_misc: ${resources.schemas.misc.name}
      libraries:
        - notebook:
            path: ../dimensions/dim_customer.py
      target: ${resources.schemas.customer_gold.name}
      development: true
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true