resources:
  dashboards:
    clientes_por_categoria:
      display_name: "[${bundle.target}] Clientes por Categoria"
      warehouse_id: ${resources.sql_warehouses.telecom_analytics_warehouse.id}
      file_path: ./clientes_por_categoria.lvdash.json
