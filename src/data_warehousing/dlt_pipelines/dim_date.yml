resources:
  pipelines:
    pipeline_dim_date:
      name: dim_date
      configuration:
        confs.p_catalog: ${var.catalog_name}
        confs.p_schema_misc: ${resources.schemas.misc.name}
      libraries:
        - notebook:
            path: ../dimensions/dim_date.py
      target: ${resources.schemas.misc.name}
      
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true
