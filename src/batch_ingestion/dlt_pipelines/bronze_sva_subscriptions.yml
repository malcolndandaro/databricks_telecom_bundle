resources:
  pipelines:
    pipeline_bronze_sva_subscriptions:
      name: bronze_sva_subscriptions
      configuration:
        confs.p_catalog: ${var.catalog_name}
        confs.p_schema_ingestion: ${resources.schemas.ingestion.name}
      libraries:
        - notebook:
            path: ../apply_changes_snapshot/sva_subscriptions/bronze_sva_subscriptions.sql
      target: ${resources.schemas.customer_bronze.name}
      
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true
