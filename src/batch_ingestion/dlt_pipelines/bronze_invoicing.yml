resources:
  pipelines:
    pipeline_bronze_invoicing:
      name: bronze_invoicing
      configuration:
        confs.p_catalog: ${var.catalog_name}
        confs.p_schema_ingestion: ${resources.schemas.ingestion.name}
      libraries:
        - notebook:
            path: ../incremental/invoicing/bronze_invoicing.py
      target: ${resources.schemas.billing_bronze.name}
      
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true
