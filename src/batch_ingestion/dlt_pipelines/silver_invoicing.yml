resources:
  pipelines:
    pipeline_silver_invoicing:
      name: silver_invoicing
      configuration:
        confs.p_catalog: ${var.catalog_name}
        confs.p_schema_billing_bronze: ${resources.schemas.billing_bronze.name}
        confs.p_schema_billing_silver: ${resources.schemas.billing_silver.name}
      libraries:
        - notebook:
            path: ../incremental/invoicing/silver_invoicing.sql
      target: ${resources.schemas.billing_silver.name}
      development: true
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true
