resources:
  pipelines:
    pipeline_dim_product:
      name: dim_product
      configuration:
        confs.p_catalog: ${var.catalog_name}
      libraries:
        - notebook:
            path: ../dimensions/dim_product.py
      target: customer_gold
      development: true
      photon: true
      channel: PREVIEW
      catalog: ${var.catalog_name}
      serverless: true