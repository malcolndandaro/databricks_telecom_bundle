resources:
  jobs:
    Task_7_bronze_silver_sva_subscriptions:
      name: "Task 7 - Bronze Silver SVA Subscriptions"
      tasks:
        - task_key: bronze_sva_subscriptions
          pipeline_task:
            pipeline_id: ${resources.pipelines.pipeline_bronze_sva_subscriptions.id}
            full_refresh: false
        - task_key: silver_sva_subscriptions
          depends_on:
            - task_key: bronze_sva_subscriptions
          pipeline_task:
            pipeline_id: ${resources.pipelines.pipeline_silver_sva_subscriptions.id}
            full_refresh: false
      tags:
        cost_center: "300.002"
        domain: customer
        env: prod
        project: financeiro
        uc_name: Ingestão
        uc_step: "1"
      queue:
        enabled: true
      parameters:
        - name: p_catalog
          default: ${var.catalog_name}
